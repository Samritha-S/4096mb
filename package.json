{
  "name": "codeimpact-backend",
  "version": "1.0.0",
  "private": true,
  "description": "CodeImpact Person 3 — Backend & LLM Layer Service",
  "scripts": {
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 3000 --reload",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 3000",
    "build": "pytest tests/ -v",
    "lint": "pytest tests/ -v"
  }
}
